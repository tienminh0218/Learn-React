import React from "react";
import PropTypes from "prop-types";

const Loading: React.FC = () => {
    return (
        <div>
            <h1>Loading...</h1>
        </div>
    );
};

export default Loading;
