import React, { FC } from "react";
import { BrowserRouter as Router, Switch, Route, Link } from "react-router-dom";

const Main: FC = () => {
    return (
        <>
            <Router></Router>
        </>
    );
};

export default Main;
